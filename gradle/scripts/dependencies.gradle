def registrate = "MC1.21-1.3.0+67" // Registrate https://maven.ithundxr.dev/#/snapshots/com/tterrag/registrate/Registrate
def emi = "1.1.22+1.21.1" // EMI https://maven.terraformersmc.com/releases/dev/emi/emi-neoforge
def jei = "24.0.0.2";

def jGraphT = "1.5.2"

def jetbrainsAnnotations = "26.0.1"

dependencies {
	// Registrate
	api("com.tterrag.registrate:Registrate:${registrate}")
	apiCompileOnly("com.tterrag.registrate:Registrate:${registrate}")
	jarJar("com.tterrag.registrate:Registrate:${registrate}")

	// EMI
	//TODO emi
//	compileOnly("dev.emi:emi-neoforge:${emi}:api")
//	runtimeOnly("dev.emi:emi-neoforge:${emi}")

	//JEI
	compileOnly("mezz.jei:jei-${minecraft_version}-neoforge-api:${jei}")
	runtimeOnly("mezz.jei:jei-${minecraft_version}-neoforge:${jei}")

	//jGraphT
	compileOnly("org.jgrapht:jgrapht-core:${jGraphT}")
	jarJar("org.jgrapht:jgrapht-core:${jGraphT}")
	additionalRuntimeClasspath("org.jgrapht:jgrapht-core:${jGraphT}")

	compileOnly("org.jetbrains:annotations:${jetbrainsAnnotations}")

	implementation sourceSets.api.output
}
